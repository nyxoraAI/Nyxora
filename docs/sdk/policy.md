# Policy SDK

**Package:** `@nyxora/policy-sdk`

The Policy SDK is the strict, cryptographic middleware that sits between the AI Brain (`core-sdk`) and the actual Private Keys (`signer-sdk`). 

> [!WARNING]
> **UNDER DEVELOPMENT**
> This package is part of the future roadmap and cannot be installed via npm yet.
